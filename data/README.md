# Datos

Coloca aquí la base de datos SQLite del catálogo Spotify:

```
data/dataset.db
```

El archivo `.db` no se sube a Git (ver `.gitignore`). Descarga o genera el dataset localmente antes de ejecutar el backend.
